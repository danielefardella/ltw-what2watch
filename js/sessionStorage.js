//document.getElementById("exampleFormControlSelect1").addEventListener("change", sessionStorage());
// We probably don't need a listener

function sessionStorage(value) {
    sessionStorage.setItem('genre', value);
}